# CLAUDE_ZH.md

本文件为 Claude Code（claude.ai/code）在此仓库中工作时提供指导。

## 仓库是什么

"MetaNode" —— 一个去中心化永续合约交易系统（教学项目）。核心模型是 **链下撮合、链上结算**：用户用 EIP-712 签署订单，relayer 撮合，只有撮合成功的 `trade()` 才上链。共三个组件：

- `perpetual-contract/` — Solidity 智能合约（Foundry）。
- `perp-backend/` — Go 后端（go-zero REST + GORM）。
- `perpetual-contract/others/fe/` — Next.js 前端。

### ⚠️ 后端存在两份拷贝

后端被签入了两次：

- `perp-backend/` — 一份不完整的快照。
- `perpetual-contract/others/backend/` — **更完整**的拷贝（额外包含 `internal/funding/`、`internal/market/`、`internal/engine/orderbook.go` + `fillsim.go` + 测试套件，以及 `internal/logic/market/` 下的 `markprice.go`、`slippage.go`、`orderpreviewlogic.go`、`riskparams.go` 等）。

修改后端代码前，先确认哪份是权威版本，并且不要只在其中一份修复 bug。前端（`perpetual-contract/others/fe/`）位于同一个 `others/` 目录树下。

## 智能合约（`perpetual-contract/`）

Foundry 项目。`foundry.toml` 使用 `solc_version = 0.8.19`，optimizer `200` runs。重映射（经 `lib/`）：`forge-std`、`@chainlink`、`@openzeppelin`。

```bash
make build          # forge build
make test           # forge test -vvv
make test-short     # forge test（简洁输出）
make test-file FILE=test/impl/DealerDecimal6Test.sol   # 单个测试文件
make test-func FUNC=testBalanceCheck                  # 单个测试函数
make test-gas       # --gas-report
make coverage       # forge coverage
make fmt            # forge fmt
make anvil          # 本地节点（会打印测试账户；RPC http://localhost:8545）
make deploy-all-local   # 本地一键部署 token + dealer + factory + emergency oracle
make deploy-dealer      # 需要 MetaNode_DEPLOYER_PK + RPC_URL 环境变量
```

**合约体积：** `MetaNodeDealer` 接近以太坊 24 KB 上限。生产/Sepolia 构建必须使用 `sepolia` profile（`FOUNDRY_PROFILE=sepolia forge build`，Makefile 脚本使用 `--via-ir`）。默认 profile **不会**启用 `via_ir`。

CI：`.github/workflows/test.yml` 在 push 时运行 `forge build --sizes` 和 `forge test -vvv`（submodules recursive）。

### 架构 —— 两个核心合约，Dealer 拆分为多个文件

- **`src/Perpetual.sol`** — 单个市场的资产负债表。交易者的仓位由 `paper`（资产数量）+ `credit` 组成；两者都可以为负（例如做多 1 BTC @ $30k → `paper=1, credit=-30000`；做空 → `paper=-1, credit=30000`）。实际 credit = `paper * fundingRate + reducedCredit`。
- **`src/MetaNodeDealer.sol`** — 拥有多个 `Perpetual` 市场。只有三种操作会改变余额：**资金费率、交易、清算**。Dealer 被拆分为共享同一合约的五个文件：
  - `MetaNodeDealer.sol`（入口）、`MetaNodeStorage.sol`（状态变量）、`MetaNodeExternal.sol`（外部函数，含 `approveTrade`）、`MetaNodeOperation.sol`（owner 专用）、`MetaNodeView.sol`（视图函数）。
- **库**（`src/libraries/`）承载核心数学逻辑：`Trading.sol`、`Liquidation.sol`、`Funding.sol`、`Position.sol`、`Operation.sol`、`Types.sol`、`Errors.sol`、`EIP712.sol`、`SignedDecimalMath.sol`。
- **预言机**（`src/oracle/`）：`ConstOracle`、`EmergencyOracle`、`OracleAdaptor`、`PythOracleAdaptor`。
- **子账户**（`src/subaccount/`）：`Subaccount.sol` + `SubaccountFactory.sol`（`newSubaccount()`）；子账户是一份合约，其 owner 是用户钱包，用于隔离风险/仓位。
- **EIP-712 订单**：domain `{name:"MetaNode", version:"1", chainId, verifyingContract: dealer}`；订单 `{perp, signer, paperAmount(int128), creditAmount(int128), info(bytes32)}`。`info` 打包了 `makerFeeRate + takerFeeRate + expiration + nonce`（各 8 字节）。Go 编码见 `perp-backend/internal/chain/eip712.go` 和 `internal/logic/auth/message.go`；测试见 `test/utils/EIP712Test.sol`。

测试布局：`test/init/TradingInit.sol`（基础 fixture：部署 USDC/MUSD、Dealer、ConstOracle、BTC-PERP + ETH-PERP，设置 order-sender），`test/impl/`（行为测试 + `Scenario*` 套件），`test/mocks/`，`test/utils/`。

## 后端（`perp-backend/` 与 `perpetual-contract/others/backend/`）

Go 1.24，module 为 `metanode`，基于 **go-zero** REST 构建。入口 `metanode.go`；用 `go run metanode.go -f etc/metanode.yaml`（或 `make run`）启动。

```bash
make gen      # goctl api go -api metanode.api -dir .  （从 .api 规范重新生成 handler/types）
make build    # go build -o bin/metanode metanode.go
make run      # go run metanode.go -f etc/metanode.yaml
make test     # go test -v ./...   （单个测试：go test ./internal/engine -run TestX）
make lint     # golangci-lint run
make fmt      # go fmt ./...
```

`metanode.api` 是 goctl API 规范 —— 请求/响应类型与路由的唯一事实来源。**永远不要手改生成的文件**（`internal/handler/*.go` 处理器、`internal/types/types.go`、`internal/handler/routes.go` —— 都标注了 "Code generated by goctl"）；应修改 `metanode.api` 并重新运行 `make gen`。业务逻辑位于 `internal/logic/<group>/<handler>logic.go`；handler 调用它。

### 后端架构

`metanode.go` 把所有东西装配进一个 `rest.Server`，然后启动长驻后台引擎：

- `engine.MatchEngine` — 内存订单簿，价格-时间优先撮合，将撮合结果提交上链。通过 `ServiceContext` 暴露为 `MatchEngine`（订单入簿，`AddOrder`）和 `OrderBook`（深度快照）。
- `engine.Liquidator` — 定期检查仓位，清算不安全仓位。
- `engine.FundingRateKeeper` — 每 8 小时结算资金费率（`FundingRate.SettleInterval`）。
- `listener.CoinbaseFeed` — Coinbase WebSocket ticker → Supabase `market_quotes`（驱动前端 realtime + 指数价；也是 `ServiceContext.IndexPrice`）。
- `listener.TreasuryDepositWatcher` — 扫描 USDC `Transfer` 到 `Ethereum.UsdcTreasuryAddress`，记入 `deposits`/`ledger_balances`（Redis 保存扫描游标）。

**链客户端是可选的。** `internal/chain.NewClient` 失败会使 `ctx.Chain = nil`；HTTP API 仍然可用（链上查询返回空/零），但撮合和资金费结算停止。配置见 `etc/metanode.yaml`（`Ethereum.RpcUrl`、`DealerAddress`、`PrivateKey` 必须与链上的 `validOrderSender` 一致）。

**两层 DB 并存**（见 `internal/svc/servicecontext.go`）。模型被拆分，因为 go-zero `sqlx` 使用 `?` 占位符，与 pgx/Postgres 不兼容：
- `go-zero sqlx`（`sqlx.SqlConn`）— `position`、`withdraw`、`funding_rate`、`liquidation`、`market`、`kline`。
- `GORM`（Supabase/Postgres）— `deposit`、`ledger_balance`、`order`、`trade`、`market_quote`（`model.NewGorm*Model`）。

DB 配置为 YAML 中的 `Supabase.DataSource`（Postgres URI，`sslmode=require`）。表在启动时由 `internal/db/migrate.go` 自动迁移；`doc/sql/schema.sql` 和 `schema/` 是参考 DDL（README 里的 `mysql -u root` 指令已过时 —— 实际数据库是 Postgres，不是 MySQL）。

鉴权：钱包 EIP-191 签名登录（`/api/v1/auth/nonce`、`/api/v1/auth/verify`）→ JWT（HS256），在 YAML 的 `Auth:` 下配置。生成路由之外的路由在 `internal/handler/wallet_auth_handlers.go` 中注册。

## 前端（`perpetual-contract/others/fe/`）

Next.js（canary）+ TypeScript + Tailwind。钱包用 `wagmi`/`viem`/`rainbowkit`；realtime 用 `@supabase/supabase-js`；图表用 `lightweight-charts`；服务端状态用 `@tanstack/react-query`。

```bash
npm run dev     # next dev
npm run build   # next build
npm run lint    # next lint
```

页面：`/`（交易界面）、`/positions`、`/faucet`。`components/` 下的组件对应后端接口（`OrderBook`、`Positions`、`FundingRatePanel` 等）。后端 base URL 和 Supabase key 来自环境变量（见 `others/fe/.env.example`）。

## 跨组件注意事项

- Sepolia 部署地址记录在 `perpetual-contract/README.md`（Dealer、BTC-PERP、ETH-PERP、USDC 地址）；后端的 `etc/metanode.yaml` 中 `Markets:` 必须指向这些地址。
- 价格在链上用定点小数表示（`SignedDecimalMath.sol`），在 API 中用十进制字符串表示（`"paperAmount"`、`"creditAmount"` 等是 `string`，不是整数）。
- 更深入的设计文档见 `perpetual-contract/docs/` 和 `perpetual-contract/others/backend/docs/`（撮合引擎、订单簿结构、资金费率实现 —— 中文）。
